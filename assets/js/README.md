# assets/js

Reserved for when `index.html`'s inline `<script>` block gets split out into standalone script file(s). Empty for now — the site still ships as a single file. See [CLAUDE.md](../../CLAUDE.md) before doing that split; it's a deliberate change, not a drive-by.
