# assets/css

Reserved for when `index.html`'s inline `<style>` block gets split out into standalone stylesheet(s). Empty for now — the site still ships as a single file. See [CLAUDE.md](../../CLAUDE.md) before doing that split; it's a deliberate change, not a drive-by.
